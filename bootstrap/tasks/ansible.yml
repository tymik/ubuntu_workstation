---
# Additional ansible packages
- name: Install all commonly used desktop packages from apt
  apt:
    name: 
      - ansible-doc
      - ansible-lint
    state: present
    update_cache: yes
    autoclean: yes
